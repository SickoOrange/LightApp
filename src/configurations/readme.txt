Sources are for generating base structure, etc.
It`s default values for that.

config.global.json is in the repository. So can't to change it.

config.local.json is NOT in the repository. It's intended for a local rewriting.